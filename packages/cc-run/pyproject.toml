[project]
name = "cc-run"
dynamic = ["version"]
description = "Run a command across many projects in parallel"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9", "rich>=13.7", "cctk-core>=0.2.0"]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "pytest-asyncio>=0.23",
    "mypy>=1.11",
    "ruff>=0.6",
]

[project.scripts]
cc-run = "orchestrator.cli:main"

[tool.uv.sources]
cctk-core = { workspace = true }

[tool.hatch.version]
path = "src/orchestrator/__init__.py"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/orchestrator"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "--cov=src/orchestrator --cov-report=term-missing --cov-fail-under=70"

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]
